# msh-Shell
Linux-based shell program using C with functionalities including accepting Linux commands, forking a child process, and running commands until the shell is exited. 
The shell program was able to respond to commands for listing files in a directory, creating and changing a directory, and listing the last 15 commands 
